// With function keyword
function addNumbers(numA, numB) {
  return numA + numB;
}

// arrow function
const addTwo = (a, b) => {
  return a + b;
};

const result = addNumbers(9, 10);

console.log(result, "============", addTwo(5, 78));
